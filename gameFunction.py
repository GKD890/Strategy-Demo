import os
from typing import Tuple
import pygame as pg

class GameFunction:
# track mouse position if in the range
    def GameMouseEvent(pos:Tuple[int,int],status:int):
        # only for rectangle check return True or False
        cur = pg.mouse.get_pos #TODO
        

    # track moust position when click


    # search method for grid map